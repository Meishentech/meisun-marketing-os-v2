# V2 Production Smoke Result - 2026-07-20

測試時間：2026-07-20 12:14 CST  
測試站台：`https://meisun-marketing-os-v2.pages.dev`  
測試帳號：`test@mcttw.com.tw`（管理者權限，可切換總經理 / 行銷總監 / 業務視角）  
依據文件：`docs/V2_PRODUCTION_SMOKE_TEST.md`

## 結論

本輪完成 production 第一輪核心 smoke test。角色切換、主要唯讀頁面、業務需求單建立 / 取消、行銷案後端建立 / 封存收尾皆已確認。

Claude Code 後續已確認「行銷案新增後 UI 沒有可靠立即顯示新增列」不是功能 bug，不需要阻塞驗收。此項可列為未來全站表單體驗優化：送出後等 `loadExistingData()` 完成再關閉 modal，或在按鈕上顯示「建立中...」直到整個流程完成。

本輪仍保留一點限制：數個深層 CRUD 流程未由本輪逐項完整跑完，需依 `docs/V2_PRODUCTION_SMOKE_TEST.md` 後續補跑完整細項驗收。

## 已確認通過

### 登入與角色

- 登入頁品牌文字可讀。
- 測試帳號登入成功。
- 可切換三個視角：總經理、行銷總監、業務。
- 三個角色頁面都能載入。
- 主要頁面未再顯示「目前已接 Supabase」或「Batch SQL」等開發提示文案。

### 總經理

- 已檢查頁面：
  - 總經理戰情室
  - 預算 / 補助 / 付款
  - 商機轉換
  - Channel 成效
  - 待決策中心
  - 週報摘要
- 從戰情室首頁進入週報摘要的入口可用。
- 週報頁有 `匯出 .txt` 與 `複製週報` 操作入口。
- 未發現舊 demo 假資料取代 live 空狀態的明顯問題。

### 行銷總監

- 已檢查頁面：
  - 行銷總監工作台
  - 行銷專案管理
  - 預算 / 補助 / 付款
  - Channel 成效
  - 招標工具管理
  - 合作廠商 / 交付物
  - 公會管理
  - 產品知識庫
  - 業務需求單
  - 週報摘要
- 行銷專案管理的跨專案巡檢入口可用：
  - 即將到期任務
  - 待付款預算
  - 專案文件
- 巡檢頁的「返回行銷專案」動線可回到行銷案列表。

### 業務

- 已檢查頁面：
  - 業務資料中心
  - 文宣 / 資源下載
  - 產品知識庫
  - 招標工具
  - 我的名單
  - 業務需求單
- 業務端產品知識庫詳情：
  - 看得到「知識條目詳情」。
  - 看不到「編輯條目」。
  - 看得到「提出補充需求」。

### 業務需求單

- 已用業務視角建立測試需求單：
  - `SMOKE 2026-07-20 業務需求單`
- 行銷總監視角可看到該需求單。
- 已用取消流程收尾，取消原因：
  - `SMOKE 驗收完成，取消測試需求。`
- 取消後該需求單不再出現在進行中清單。

### 行銷案主檔

- 已用行銷總監視角送出兩筆測試行銷案：
  - `SMOKE 2026-07-20 行銷案 C`
  - `SMOKE 2026-07-20 行銷案 D`
- 透過 live Supabase REST 查詢確認兩筆資料確實建立成功。
- 已用 soft archive 收尾，兩筆 `archived_at` 皆已回填。
- 封存原因：
  - `SMOKE production 驗收完成，自動封存測試行銷案。`

### Live SQL / Schema

- Batch 13B / 14B / 14C live SQL 已在前一輪用 live REST schema 查詢確認欄位存在。
- 風險 smoke test 曾回傳實際筆數：
  - `risk_count = 12`
  - `risk_update_count = 5`
- Batch 14C smoke test 曾回傳：
  - `performance_count = 1`

## 需要複查

### 1. 本輪尚未逐項完成的深層 CRUD

以下流程本輪尚未完整逐項跑完，因此不能在本輪標為全數通過：

- 任務新增 / 編輯 / 取消。
- 預算項目新增 / 編輯 / 取消與支出彙總交叉檢查。
- 文件上傳、signed URL 開啟、新版本、封存。
- 風險新增 / 追蹤紀錄新增 / 編輯 / 取消 / 封存。
- 成效資料新增 / 編輯 / Channel 頁回讀。
- 合作廠商 / 交付物新增 / 編輯 / 取消。
- 文宣資源新增 / 編輯 / 封存與業務端回讀。
- 產品知識庫新增 / 編輯 / 連結 / 移除資源。

### 2. 手機 375 x 812

- 先前已針對全站手機橫向爆版問題修正 `.app-shell` grid。
- 本輪 in-app browser 未提供可靠 viewport 切換能力，因此本輪未重新完整跑 375 x 812 的全部流程。
- 需要另用可固定 viewport 的瀏覽器工具補測。

## 非阻塞優化

- 全站表單目前多數流程是資料寫入後關閉 modal，再重新載入資料。若要讓真人操作與未來自動化測試更清楚，可另開小 batch，改成按鈕顯示「建立中... / 儲存中...」，並等重新載入完成後再關閉 modal。

## 建議下一步

1. 照 `docs/V2_PRODUCTION_SMOKE_TEST.md` 補跑深層 CRUD 細項。
2. 用可固定 375 x 812 viewport 的方式補跑手機流程，尤其是行銷案詳情中的任務 / 預算 / 文件 / 風險 / 成效 modal。
3. 完成後補一份最終完整 production smoke result。
